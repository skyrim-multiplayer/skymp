{
  "name": "skymp5-chat",
  "version": "1.0.0",
  "description": "Minimal SkyMP client UI replacement focused on chat and commands",
  "license": "GPL-3.0",
  "scripts": {
    "build": "node scripts/build-ui.mjs",
    "check": "tsc --noEmit",
    "deploy:ui": "node scripts/deploy-ui.mjs",
    "deploy:server": "node scripts/deploy-server.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
