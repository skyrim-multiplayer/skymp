// eslint-disable-next-line @typescript-eslint/no-explicit-any
type ClientApi = any;
// eslint-disable-next-line no-var
declare var client: ClientApi;

/*const cmd = client.sp.findConsoleCommand(" ConfigureWB");
cmd.shortName = cmd.longName = "mp";
cmd.execute = */
// WIP
