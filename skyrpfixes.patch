diff --git a/.gitignore b/.gitignore
index a6aa06e..9d61765 100644
--- a/.gitignore
+++ b/.gitignore
@@ -1,10 +1,10 @@
 # ── Source directories ─────────────────────────────────────────────────────────
 # Client repo is cloned here by `npm run setup` — not tracked in this repo.
-sources/client/
+skymp5-backend/sources/client/
 
 # SKSE binaries are large and placed manually — only the README is tracked.
-sources/skse/*.exe
-sources/skse/*.dll
+skymp5-backend/sources/skse/*.exe
+skymp5-backend/sources/skse/*.dll
 
 # ── Generated output ───────────────────────────────────────────────────────────
 # Both files are rebuilt by `npm run merge` — do not track them in git.
@@ -175,8 +175,8 @@ package-lock.json
 
 
 package-lock.json
-sources/skse/Data/Platform/Distribution/RuntimeDependencies/libcef.dll
-sources/skse/Data/Platform/Distribution/RuntimeDependencies/libnode.dll
+skymp5-backend/sources/skse/Data/Platform/Distribution/RuntimeDependencies/libcef.dll
+skymp5-backend/sources/skse/Data/Platform/Distribution/RuntimeDependencies/libnode.dll
 nssm.zip
 /nssm-extracted
 
diff --git a/skymp5-backend/.env.example b/skymp5-backend/.env.example
index 21895db..8345b25 100644
--- a/skymp5-backend/.env.example
+++ b/skymp5-backend/.env.example
@@ -21,7 +21,7 @@ SKYMP_PORT=7777
 # ── Server metadata ───────────────────────────────────────────────────────────
 # These values are returned by GET /api/serverinfo and GET /api/servers.
 # Keep them in sync with the game server's own server-settings.json.
-SERVER_NAME=Frostfall Roleplay
+SERVER_NAME=Skyrim Roleplay
 SERVER_MAX_PLAYERS=100
 # Set to false to use online mode (session tokens via this backend).
 # Must match offlineMode in the game server's server-settings.json.
@@ -65,9 +65,9 @@ DASHBOARD_API_BASE_URL=https://api.example.com
 DASHBOARD_DISCORD_IDS=
 # Public URL of the website — used to redirect back after dashboard OAuth.
 WEBSITE_URL=https://your-website.example.com
-# URL of the local Frostfall-Admin service (never expose to the internet).
+# URL of the local SkyMP-Admin service (never expose to the internet).
 ADMIN_URL=http://localhost:5001
-# Shared secret between the backend and the admin service (must match ADMIN_TOKEN in Frostfall-Admin/.env).
+# Shared secret between the backend and the admin service (must match ADMIN_TOKEN in SkyMP-Admin/.env).
 ADMIN_TOKEN=
 
 # ── Metrics HTTP auth ─────────────────────────────────────────────────────────
diff --git a/skymp5-backend/data/news.json b/skymp5-backend/data/news.json
index 02c59f0..b322fb6 100644
--- a/skymp5-backend/data/news.json
+++ b/skymp5-backend/data/news.json
@@ -1,7 +1,7 @@
 [
   {
     "title": "The Launcher Has Arrived",
-    "body":  "The Frostfall launcher is now available. Stay tuned for further updates.",
+    "body":  "The Skyrim Roleplay launcher is now available. Stay tuned for further updates.",
     "date":  "Apr 14, 2026",
     "tag":   "UPDATE",
     "image": "/images/news-launcher-release.gif"
diff --git a/skymp5-backend/routes/admin.js b/skymp5-backend/routes/admin.js
index 19fd5c5..2b55004 100644
--- a/skymp5-backend/routes/admin.js
+++ b/skymp5-backend/routes/admin.js
@@ -1,6 +1,6 @@
 'use strict'
 // ── Admin proxy ───────────────────────────────────────────────────────────────
-// Validates the bearer token, then forwards requests to the Frostfall-Admin
+// Validates the bearer token, then forwards requests to the SkyMP-Admin
 // service. Keeps the admin service off the public internet — only the backend
 // is exposed; the admin process binds to localhost only.
 
diff --git a/skymp5-backend/routes/dashboard-auth.js b/skymp5-backend/routes/dashboard-auth.js
index c41913c..1240262 100644
--- a/skymp5-backend/routes/dashboard-auth.js
+++ b/skymp5-backend/routes/dashboard-auth.js
@@ -4,7 +4,7 @@
 // dashboard session tokens instead of launcher session tokens.
 //
 // Required Discord app settings:
-//   Redirects: add DISCORD_DASHBOARD_REDIRECT_URI (e.g. https://api.frostfall.online/auth/dashboard/callback)
+//   Redirects: add DISCORD_DASHBOARD_REDIRECT_URI (e.g. https://api.skyrimroleplay.co.uk/auth/dashboard/callback)
 
 const { Router }              = require('express')
 const https                   = require('https')
diff --git a/skymp5-backend/routes/files.js b/skymp5-backend/routes/files.js
index c4d403d..3ac27ec 100644
--- a/skymp5-backend/routes/files.js
+++ b/skymp5-backend/routes/files.js
@@ -16,7 +16,7 @@ const path   = require('path')
 const fs     = require('fs')
 const rateLimit = require('express-rate-limit')
 
-const ZIP_PATH     = path.join(__dirname, '..', 'public', 'files', 'frostfall-client.zip')
+const ZIP_PATH     = path.join(__dirname, '..', 'public', 'files', 'skyrp-client.zip')
 const VERSION_PATH = path.join(__dirname, '..', 'data', 'files-version.json')
 
 const NOT_BUILT = { error: 'File package not found. Run `npm run merge` on the server first.' }
@@ -51,7 +51,7 @@ router.get('/zip', (req, res) => {
   const stat = fs.statSync(ZIP_PATH)
   res.setHeader('Content-Type', 'application/zip')
   res.setHeader('Content-Length', stat.size)
-  res.setHeader('Content-Disposition', 'attachment; filename="frostfall-client.zip"')
+  res.setHeader('Content-Disposition', 'attachment; filename="skyrp-client.zip"')
 
   const stream = fs.createReadStream(ZIP_PATH)
   stream.on('error', () => res.destroy())
diff --git a/skymp5-backend/routes/webhook.js b/skymp5-backend/routes/webhook.js
index c2d02a5..a7f8505 100644
--- a/skymp5-backend/routes/webhook.js
+++ b/skymp5-backend/routes/webhook.js
@@ -5,7 +5,7 @@
  * Route: POST /webhooks/github
  *
  * GitHub delivers a signed JSON payload whenever a push is made to the
- * Frostfall-Client repository.  This handler:
+ * SkyRP-Client repository.  This handler:
  *   1. Verifies the HMAC-SHA256 signature (X-Hub-Signature-256 header).
  *   2. Responds 200 immediately so GitHub doesn't time out.
  *   3. Runs `git pull` in sources/client/ then rebuilds public/files/root/.
@@ -15,7 +15,7 @@
  *   GITHUB_WEBHOOK_SECRET  (required)
  *     The same secret you enter in the GitHub repo → Settings → Webhooks form.
  *
- * GitHub webhook setup (Frostfall-Client repo)
+ * GitHub webhook setup (SkyRP-Client repo)
  * ─────────────────────────────────────────────
  *   Payload URL : https://<your-server>/webhooks/github
  *   Content type: application/json
diff --git a/skymp5-backend/scripts/merge-files.js b/skymp5-backend/scripts/merge-files.js
index e08fc77..2d8235f 100644
--- a/skymp5-backend/scripts/merge-files.js
+++ b/skymp5-backend/scripts/merge-files.js
@@ -5,7 +5,7 @@
  * that the launcher downloads, and builds the distributable zip.
  *
  *   sources/client/  →  public/files/root/
- *                    →  public/files/frostfall-client.zip
+ *                    →  public/files/skyrp-client.zip
  *                    →  data/files-version.json
  *
  * SKSE is NOT included here — it is managed by the user via the Vortex collection.
@@ -23,7 +23,7 @@ const ROOT = path.join(__dirname, '..')
 
 const CLIENT_SRC   = path.join(ROOT, 'sources', 'client')
 const OUTPUT_DIR   = path.join(ROOT, 'public', 'files', 'root')
-const ZIP_PATH     = path.join(ROOT, 'public', 'files', 'frostfall-client.zip')
+const ZIP_PATH     = path.join(ROOT, 'public', 'files', 'skyrp-client.zip')
 const VERSION_FILE = path.join(ROOT, 'data', 'files-version.json')
 
 // ── Version helpers ───────────────────────────────────────────────────────────
diff --git a/skymp5-backend/scripts/setup-client.js b/skymp5-backend/scripts/setup-client.js
index bcd3b7c..bdb7834 100644
--- a/skymp5-backend/scripts/setup-client.js
+++ b/skymp5-backend/scripts/setup-client.js
@@ -1,9 +1,9 @@
 'use strict'
 
 /**
- * One-time (and subsequent) setup for the Frostfall-Client source directory.
+ * One-time (and subsequent) setup for the SkyRP-Client source directory.
  *
- *   First run  → git clone  https://github.com/F02K/Frostfall-Client.git  sources/client/
+ *   First run  → git clone  https://github.com/SkyrimRoleplay/SkyRP-Client.git  sources/client/
  *   Later runs → git pull --ff-only  (fast-forward only, refuse dirty merges)
  *
  * After the clone / pull the merge pipeline runs automatically so that
@@ -17,7 +17,7 @@ const { execFileSync } = require('child_process')
 const path = require('path')
 const fs   = require('fs')
 
-const CLIENT_REPO = 'https://github.com/F02K/Frostfall-Client.git'
+const CLIENT_REPO = 'https://github.com/SkyrimRoleplay/SkyRP-Client.git'
 const CLIENT_DIR  = path.join(__dirname, '..', 'sources', 'client')
 
 // ── Clone or pull ─────────────────────────────────────────────────────────────
@@ -36,7 +36,7 @@ if (isCloned) {
     process.exit(1)
   }
 } else {
-  console.log('[setup] Cloning Frostfall-Client repo…')
+  console.log('[setup] Cloning SkyRP-Client repo…')
   console.log('[setup]   →', CLIENT_DIR)
   try {
     execFileSync('git', ['clone', CLIENT_REPO, CLIENT_DIR], { stdio: 'inherit' })
diff --git a/skymp5-launcher/.env.example b/skymp5-launcher/.env.example
index a1440f8..d6700e4 100644
--- a/skymp5-launcher/.env.example
+++ b/skymp5-launcher/.env.example
@@ -1,5 +1,5 @@
 # ── Backend API ───────────────────────────────────────────────────────────────
-# Base URL of the Frostfall backend server.
+# Base URL of the Skyrim Roleplay backend server.
 # The launcher fetches the server list, modlist, news, and client files from here.
 # Change this to your production URL when building a release.
-API_URL=https://api.frostfall.online
\ No newline at end of file
+API_URL=https://api.skyrimroleplay.co.uk
\ No newline at end of file
diff --git a/skymp5-launcher/modlist-example.txt b/skymp5-launcher/modlist-example.txt
index 67da877..434f3f2 100644
--- a/skymp5-launcher/modlist-example.txt
+++ b/skymp5-launcher/modlist-example.txt
@@ -1,4 +1,4 @@
-# Frostfall Launcher — Modlist API Response Format
+# SkyRP Launcher — Modlist API Response Format
 #
 # When the backend endpoint is ready, GET /api/modlist should return a JSON
 # array matching the shape below.  The renderer's loadModlist() will swap out
