## Support opening url in os default browser

Now we can open url in external os default browser.

```typescript
browser.shellExecute("https://google.com");
```

Only `https://` prefix urls are allowed.
